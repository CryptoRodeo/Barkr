<h2>Update Tweet<h2>

<%= 

    form_for :editable_tweet, "/tweets/#{editable_tweet.id}", method: :patch, values: {editable_tweet: editable_tweet} do 
            div class: 'input' do
                label :username
                text_field :username
            end
            div class: 'input' do
                label :content
                text_area :content
            end

            div class: 'controls' do
            submit 'Update Tweet', class: 'button-primary'
            end
    end

%>